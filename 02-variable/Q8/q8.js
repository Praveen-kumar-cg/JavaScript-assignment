let message;
console.log(`Message before assign:- ${message}`);
message="Hello,World!!";
console.log(`Message after assign value:- ${message}`)

let name="praveen kumar"; let age=18; let isStudent=true;
const MAX_USER=100
console.log(`name is ${name} and age is ${age} and is student ${isStudent} and max user are allowed ${MAX_USER}`)